# You should have already followed instructions in the INSTALL document

# compile core
cd client
autoreconf -ivf  # this only needs to be executed the first time (it generates the configure script)
./configure      # this generates the make files
make
cd ..

# compile the Qt GUI
#cd qtgui
#make
#cd ..
